# Example / optional CoA home_server.
# Do NOT commit real NAS IPs or shared secrets here.
# Prefer outbound CoA from billing scripts (coa_*.sh) or the backend,
# which read secrets from the `nas` table / env at runtime.
#
# To enable proxy CoA, set COA_NAS_IP and COA_NAS_SECRET via env and
# template this file at container start — never hardcode production secrets.

# home_server coa_server {
#     type = coa
#     ipaddr = ${COA_NAS_IP}
#     port = 1700
#     secret = ${COA_NAS_SECRET}
#     # Prefer yes for NAS that support Message-Authenticator (Blast RADIUS).
#     require_message_authenticator = yes
#     response_window = 20
# }
#
# home_server_pool coa_pool {
#     type = fail_over
#     home_server = coa_server
# }
